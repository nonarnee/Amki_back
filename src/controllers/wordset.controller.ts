import {NextFunction, Request, Response} from 'express';

const wordsetController = {
    getWordset: async (req: Request, res: Response, next: NextFunction) => {

    },
    getWordsetById: async (req: Request, res: Response, next: NextFunction) => {

    },
};

export default wordsetController;
